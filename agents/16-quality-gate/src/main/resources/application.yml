spring:
  application:
    name: 16-quality-gate
  main:
    banner-mode: "off"
    web-application-type: none
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o

logging:
  level:
    com.example.agents.qualitygate: INFO
    org.springframework.boot: WARN
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
